# Discord × GPT-Live Voice Bot
## Concept Specification

## 概要

Discordのボイスチャンネル上で、ユーザーがAIと自然に会話できる音声Botを作る。

このプロジェクトの目的は、単なる「音声入力対応チャットBot」ではない。

目指すのは、Discord上でBotと同じVCに入り、**普通に人と通話しているようなテンポでAIと話せる体験**。

OpenAIのリアルタイム音声モデルを利用し、音声入力・応答生成・音声出力をストリーミングで処理する。

---

## 体験イメージ

ユーザーがDiscordのVCに入り、Botを呼ぶ。

Botも同じVCに参加する。

その後はコマンド操作を意識せず、そのまま会話する。

```text
User:
「なあ、明日の予定って」

AI:
「午後なら──」

User:
「あ、午後じゃなくて夜」

AI:
「夜ね。なら──」
```

AIが喋っている途中でもユーザーは普通に割り込める。

ユーザーが割り込んだ瞬間、AIは発話をやめ、新しい発言を聞く。

この「会話のテンポ」をプロジェクトで最も重要な品質とする。

---

## 最重要コンセプト

### Real-time First

音声を録音してから処理する方式にはしない。

以下のような処理は避ける。

```text
ユーザーが話す
↓
話し終わる
↓
録音完了
↓
音声認識
↓
AI生成
↓
音声生成
↓
再生
```

代わりに、ユーザーが話している最中から音声をAIへ送り続ける。

AIの回答も、完成するまで待たず、生成された音声から順次Discordへ流す。

つまり、

```text
Input Streaming
↓
Realtime AI
↓
Output Streaming
```

を基本思想とする。

---

## Conversation First

このBotではAIの回答精度だけでなく、

- 返事を始めるまでの速さ
- ユーザーの発話終了を自然に判断できること
- AIが喋りすぎないこと
- ユーザーが自然に割り込めること
- 割り込み後に古い音声が残らないこと

を非常に重要視する。

「賢いけど会話しづらいAI」より、

**多少短い回答でもテンポよく話せるAI**

を優先する。

---

## Interruption / Barge-in

AIの発話中でも、ユーザーはいつでも話し始められる。

ユーザーの新しい発話が検知された場合、

```text
AI発話中
↓
ユーザーが話し始める
↓
AIの現在の発話を即座に停止
↓
ユーザーの音声を優先
↓
新しい内容に応答
```

とする。

AIの生成だけ止めるのではなく、既に再生待ちになっている音声も破棄する。

割り込み後にAIが数秒喋り続ける状態は許容しない。

---

## Turn Detection

単純な「一定時間無音だったら発話終了」だけに依存しない。

例えば、

```text
「そうだな……」

「明日にしよう」
```

のような自然な間を、発話終了と誤判定しないことが望ましい。

利用可能であれば、リアルタイム音声モデル側のVADやsemantic turn detectionなどを活用する。

ターン判定は会話品質に直結するため、重要な調整対象とする。

---

## Latency

このプロジェクトでは低遅延を優先する。

特に測るべきなのは、

```text
ユーザーが話し終わる
↓
AIの最初の音声が聞こえる
```

までの時間。

また、

```text
ユーザーが割り込む
↓
AIの音声が止まる
```

までの時間も重要。

音声品質を上げるために大きなバッファを作り、会話が遅延する設計は避ける。

多少の音声欠損より、リアルタイム性を優先する場面があってよい。

---

## Discord上での利用方法

MVPでは1対1の通話を想定する。

```text
Discord Voice Channel

User
+
AI Bot
```

ユーザーがVCに入り、Botを参加させる。

Botはそのユーザーの音声のみをAIへ渡し、AIの音声をVCへ再生する。

複数ユーザーの同時会話は初期スコープに含めない。

---

## Session

Discordの通話中は、AIとのリアルタイムセッションを維持する。

ユーザーが発言するたびに新しいAPIリクエストを作るのではなく、

```text
Discord Call
=
1 continuous AI voice session
```

という考え方にする。

これにより、会話の文脈を維持しながら自然な連続会話を行う。

---

## AI Personality

AIはテキストチャットよりも簡潔に話す。

音声会話では長文回答を避け、

- 短めに返す
- 必要なら続きを話す
- 相手の発話を待つ
- 不要な説明を始めない
- 自然な相槌を使う
- 会話を独占しない

ことを重視する。

「文章として完璧な回答」より「話していて自然な回答」を優先する。

---

## MVP

最初のバージョンでは以下だけ実現する。

```text
Discord VC
      ⇅
    AI Bot
      ⇅
OpenAI realtime voice model
```

必要な機能:

- BotがVCへ参加できる
- ユーザー音声をリアルタイムで取得できる
- 音声をリアルタイムAIへ送れる
- AI音声をストリーミング再生できる
- 会話の文脈を維持できる
- ユーザーがAIへ割り込める
- 通話を開始・終了できる

---

## MVPで作らないもの

初期段階では以下は不要。

- Web UI
- 管理画面
- DB
- 会話履歴保存
- 長期記憶
- 複数VC対応
- 複数ユーザー会話
- 話者識別
- Discordサーバー公開Bot化
- 複雑な権限管理
- AI tools
- Web検索
- カレンダー
- PC操作
- 外部サービス連携

まず音声会話自体の品質を完成させる。

---

## 将来拡張

音声通話が十分自然になった後、

```text
AI Voice
  │
  ├ Discord操作
  ├ Web検索
  ├ カレンダー
  ├ メモリ
  ├ PC操作
  ├ Home Server
  └ その他のTools
```

へ拡張可能にする。

将来的にはDiscord上のAI秘書・AIアシスタントの音声インターフェースとして利用できる構造を想定する。

ただし、初期実装では拡張性を理由に過剰設計しない。

---

## 優先順位

品質の優先順位は以下。

```text
1. 会話テンポ
2. 割り込み性能
3. 発話終了判定
4. 安定性
5. 音声品質
6. AI回答品質
7. 拡張性
```

機能数ではなく、

**「Discordに入ってAIと話した瞬間に、ちゃんと通話っぽい」**

ことを成功条件とする。

---

## 成功条件

MVP完成時に、ユーザーが技術的な仕組みを意識せず、

```text
「これ普通にAIと通話してるな」
```

と感じられること。

もしその感覚が出ていない場合、機能追加はせず、

- latency
- buffering
- turn detection
- interruption
- audio streaming

を優先して改善する。

このプロジェクトでは、機能の多さより音声会話体験そのものを完成させる。